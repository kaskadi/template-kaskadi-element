name: build-on-firefox
on:
  push:
    paths:
      - '**.js'
      - 'example/index.html'
jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1
    - name: test edge
      run: |
        npm i
        npm run test.Edge
